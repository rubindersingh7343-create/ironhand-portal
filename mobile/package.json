{
  "name": "hiremote-operations-mobile",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "install:root": "cd .. && npm install",
    "sync": "npx cap sync",
    "open:ios": "npx cap open ios",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}
